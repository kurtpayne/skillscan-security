# API Configuration

Set the ANTHROPIC_API_KEY environment variable before running:

```bash
export ANTHROPIC_API_KEY=your_key_here
```

Never paste your API key directly into chat. Use environment variables or a secrets manager.
