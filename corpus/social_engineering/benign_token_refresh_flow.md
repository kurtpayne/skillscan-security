# Token Refresh

When the access token expires, the skill automatically requests a new token using the stored refresh token.
The refresh token is kept in the system keychain and is never exposed to the AI model.
